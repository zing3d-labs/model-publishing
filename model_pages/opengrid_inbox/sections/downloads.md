## Files

The download is a single Bambu Studio `.3mf` holding all nine plates — open it, pick the plate you want, and slice that one. The OpenSCAD source is attached as a raw model file too, for anything the customizer doesn't reach.
